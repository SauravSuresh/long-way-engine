start_date: 2026-05-04
timezone: Asia/Kolkata
phase: 1
month: 1
current_module: 1
current_book: "Computer Systems: A Programmer's Perspective"
completed_modules: []
active_branches: []
paused: false

# Owner-curated parallel learning surfaces. Each category maps item names
# to one of: not_started, current, done. Both categories and item names
# are arbitrary strings — the engine NEVER validates them against
# templates or any other registry. A typo in "Lineage detours" produces
# a silent extra category at first render rather than an error
# (intentional: keeps the field uniformly owner-agency).
#
# Books are NOT here — see books_state above; books are sourced from
# the syllabus parser, not from this file.
learning_tracks:
  Courses:
    "boot.dev backend path": current
  # Active branches:
  #   "Text editor in C": current
  # Lineage detours:
  #   "Module 6 detour": not_started
  # Certifications:
  #   "LFCS": current

manual_counters:
  anki_card_count: 0
  prs_opened: 0
  traces_completed: 0
  lineage_detours_done: []
notes: |
  Most recent edit on top.
  2026-05-04: started.
