timezone: Asia/Kolkata
manual_counters:
  anki_card_count: 0
  prs_opened: 0
  traces_completed: 0
  lineage_detours_done: []
notes: 'Most recent edit on top.

  2026-08-06: fresh start now that the rung system is properly set up (48
  briefs, lw CLI, deadlines). start_date -> 2026-08-06 (Thursday, mid-week
  start is deliberate) for all three syllabuses. Marketplace-builder ONLY:
  RentalBuilder Todoist project wiped (3 stale tasks incl. the old vague
  rung-1 brief) and its cache namespace dropped, so today''s run recreates
  the rung task with brief links + deadline_date. devops-ready cache kept
  intact — its once-per-module tasks are unchanged and stay valid.

  2026-08-04: owner could not start today — start_date -> 2026-08-05 (Wednesday)
  for all three syllabuses (same cache-intact pattern; 08-04 entries pre-date
  start_date so walkers ignore them). long-way disabled in config (was paused
  but still rendered) and removed from priority_order, so disabled syllabuses
  no longer appear on the dashboard.

  2026-08-03: the 08-02 restructure was never pushed — remote daily runs
  (07-06..08-03) kept advancing the old devops-ready/long-way schedule, so
  Monday ran on stale curriculum. Merged origin/main (kept marketplace-builder
  curriculum + config; kept remote''s cache/LOG/reflections/dashboard) and
  moved the full reset forward: start_date -> 2026-08-04 (Tuesday) for all
  three syllabuses; month/phase/module -> 1. Same cache-intact pattern —
  entries up to 08-03 pre-date start_date, walkers ignore them.

  2026-08-02 (later): post-grilling restructure + full reset. marketplace-builder
  is now two tracks: Mon/Wed = 16-rung ladder (pick 1 of 3 challenges per rung,
  real consumer required, polyglot allowed); Saturday = the real rental platform
  from week 1 (first-12-sessions checklist; exam gates 1-4 live there). New
  advisory /ladder-review skill (verdict committed to repo). Crunch rule: Anki +
  one build session + Sunday review is a valid week. FULL reset: start_date ->
  2026-08-02 for ALL syllabuses (marketplace-builder, devops-ready, and paused
  long-way); month/phase/module -> 1; counters 0. Caches left intact per the
  established pattern — old entries pre-date start_date so streak/adherence
  walkers ignore them and the dashboard reports day 1 for everything.

  2026-08-02: marketplace-builder replaces devops-ready as the focus track
  (build-first syllabus; booking platform is the spine; SAA cert = parallel
  theory track months 1-3, CKA parked post-launch). devops-ready disabled in
  config (superseded — cert goals live on inside marketplace-builder; files
  kept as reference). long-way paused (paused_since 2026-08-02, parked until
  platform launch).

  2026-07-01: focus switch to devops-ready. boot.dev now every weekday; LeetCode
  alternate days (Mon/Wed/Fri); morning study Mon-Fri; Saturday = long-way CS:APP
  long reading (AM) + devops project block (PM). long-way relegated to Saturdays
  only. start_date->2026-07-01 for both (fresh slate; new template ids have no
  cache history, so old dates would false-read as missed). Streaks: devops grades
  morning-study + Anki; long-way grades Saturday reading + Anki. Common-tasks spec
  drafted (specs/common-tasks.md) but deferred.

  2026-06-21: full tracking reset for both tracks (owner request). start_date->2026-06-21,
  month/phase/module->1 for long-way and devops-ready. Adherence/streaks/last-7 start
  fresh from today. Cache left intact (cron idempotency ledger); old entries pre-date
  start_date so the streak walkers ignore them.

  2026-05-05: starting day 1. May 4 setup tasks wiped via --cleanup-project; cache
  reset.

  '
